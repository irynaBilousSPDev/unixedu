window.initAccordions = function initAccordions() {
  const accordions = document.querySelectorAll('[data-accordion]');
  if (!accordions.length) return;

  // Placeholder for future accordion behavior.
};

